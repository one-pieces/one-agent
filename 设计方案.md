# one-agent 设计方案

> 版本：v0.1（初稿）
> 日期：2026-08-09
> 状态：待评审
> 关联：立项.md（已移回 Obsidian 学习库：`obsidian/Learn 学习库/one-agent/立项.md`）

---

## 目录

1. [背景与动机](#1-背景与动机)
2. [目标与非目标](#2-目标与非目标)
3. [现状分析：eve 的痛点](#3-现状分析eve-的痛点)
4. [技术选型分析](#4-技术选型分析)
5. [总体架构](#5-总体架构)
6. [内核层设计（agent-core）](#6-内核层设计agent-core)
7. [应用层设计（one-agent-app）](#7-应用层设计one-agent-app)
8. [数据模型与流协议](#8-数据模型与流协议)
9. [项目目录结构（根目录多项目文件夹）](#9-项目目录结构根目录多项目文件夹)
10. [里程碑规划](#10-里程碑规划)
11. [风险与应对](#11-风险与应对)

---

## 1. 背景与动机

现有项目 `eve-agent`（`~/codes/eve-agent`）基于 Vercel 的 **eve 框架**（`eve@0.18.2`）构建。eve 是一个"文件系统优先"（filesystem-first）的 agent 框架：agent 的模型、工具、指令全部以 `agent/` 目录下的**静态文件**形式定义，`defineAgent()` 在模块加载时解析配置。

实际使用中发现：**eve 无法在运行时动态配置 model 和 tools 等 agent 设置**。为了在 eve-agent 中实现"按请求/按会话切换模型"，不得不写出一系列 hack（见 [[#3 现状分析：eve 的痛点]]），维护成本高且脆弱。

因此决定新建 **one-agent** 项目：从调用大语言模型提供商底层接口开始，**从零重写一个 agent 内核**，把"model / tools / 指令"等配置变成**一等公民的运行时数据**，天然支持动态配置；上层再构建一个应用层（Next.js）提供 UI、对话和 Agent 管理能力。

---

## 2. 目标与非目标

### 目标

- ✅ 重写 agent 内核：自研 provider 调用层（OpenAI 兼容 + Anthropic 原生），不依赖任何重型 agent 框架
- ✅ 动态配置：model、tools、instructions、记忆策略等全部为可序列化 JSON 数据，支持运行时创建/修改/热更新 Agent 实例，支持每会话/每请求 override
- ✅ 双项目结构：`agent-core`（内核层，纯逻辑，框架无关）+ `one-agent-app`（应用层，Next.js UI + 对话 + Agent 管理）
- ✅ 流式输出端到端：内核 → 应用层 → 浏览器，全程流式（SSE）
- ✅ 工具系统可扩展：内置工具注册表 + MCP 工具桥接

### 非目标（v0.1 不做）

- ❌ 不做 eve 的"文件系统即 Agent"（目录契约）模式 —— 配置走数据，不走文件
- ❌ 不做 Workflow SDK 级别的可重放持久化执行（durable execution）—— 先用简单会话持久化
- ❌ 不做多 agent 编排框架（subagent 调度、多 agent 协作图）—— 内核预留扩展点即可
- ❌ 不做代码沙箱（sandbox）的完整实现 —— 工具执行沙箱放到 M1 之后
- ❌ 不追求生产级多租户权限体系 —— 应用层先做单用户/简单多用户

---

## 3. 现状分析：eve 的痛点

### 3.1 eve 是什么

eve（npm `eve`，2026-08 最新 0.31.3，仍为 preview）是 **Vercel 出品**的"文件系统优先" agent 框架，基于 Vercel AI SDK（`ai`）+ Workflow SDK 构建。Agent 即目录：

```
agent/
├── agent.ts           # defineAgent({ model, reasoning, compaction, limits, ... }) —— 静态配置
├── instructions.md    # 常驻指令
├── tools/             # tools/*.ts —— 静态工具文件
├── skills/ hooks/ channels/ connections/ sandbox/ subagents/ schedules/
```

### 3.2 核心痛点：配置是"编译期"的，不是"运行期"的

| 维度 | eve 的行为 | 后果 |
|---|---|---|
| 模型配置 | `agent.ts` 中 `defineAgent({ model })`，模块加载时解析 | 换模型 = 改代码；动态换模型要靠 hack |
| 工具配置 | `tools/*.ts` 文件，构建期收集 | 无法运行时动态增删工具 |
| 每请求/每会话配置 | 不支持 | 多用户不同模型几乎不可能 |
| 框架稳定性 | preview，API 可能随时变 | 升级成本高（0.18 → 0.31 已多轮破坏性变更） |

### 3.3 eve-agent 中的 hack 证据（来自现有代码）

在 `eve-agent` 中，为了动态换模型被迫实现：

1. **AsyncLocalStorage 传配置**（`lib/model-context.ts`）：把 per-request 模型配置塞进 ALS，还要用 `Symbol.for` 全局共享实例，否则 Next.js（应用）与 Nitro（agent 运行时）双 bundle 各自持有 ALS 实例导致穿透失败；
2. **全局 pending 变量兜底**（`setPendingModelConfig/consumePendingModelConfig`）：因为 Workflow SDK 的异步执行边界不保留 ALS 上下文；
3. **缓存重建 agent 配置**（`agent/agent.ts`）：`createAgentConfig()` 默认导出函数 + `_cachedConfig`，还要处理 workflow replay 时的 `REPLAY_DIVERGENCE`；
4. **注释里写满了"workaround"** —— 这些 hack 是框架静态配置模型的直接产物。

> **结论：框架的"静态配置 + 文件优先"设计理念与"动态配置"需求根本冲突，这正是从零重写内核的理由。**

---

## 4. 技术选型分析

### 4.1 调研范围与数据来源

调研了 2026 年 8 月 JS/TS agent 生态，关键包版本已在本地核实（`npm view`，2026-08-09）：

| 包 | 版本 | 许可证 |
|---|---|---|
| `ai`（Vercel AI SDK） | 7.0.58 | Apache-2.0 |
| `mastra` | 1.23.0 | Apache-2.0 |
| `@langchain/langgraph` | 1.4.9 | MIT |
| `@openai/agents` | 0.14.3 | MIT |
| `@modelcontextprotocol/sdk` | 1.30.0 | MIT |
| `zod` | 4.4.3 | MIT |
| `@sinclair/typebox` | 0.34.52 | MIT |
| `valibot` | 1.4.2 | MIT |
| `eventsource-parser` | 3.1.0 | MIT |
| `next` | 16.3.0 | MIT |
| `turbo` | 2.10.9 | MIT |
| `eve` | 0.31.3 | Apache-2.0（仍 preview） |

### 4.2 候选框架对比（针对"内核层"选型）

| 维度 | LangGraph.js | Mastra | OpenAI Agents SDK (JS) | Vercel AI SDK | **纯自研（推荐）** |
|---|---|---|---|---|---|
| 定位 | 图运行时构建块 | 全栈 agent 框架 | 完整轻量 runtime | Provider/流式构建块 | 自研 thin core |
| agent loop 可定制性 | 高（自己搭图） | 中（内置 loop） | 低（内部固定） | 无（只有原语） | **完全掌控** |
| 动态 model/tools | ✅ config 传入 | ✅ 类实例 | ✅ 公开字段 | ✅ 每次调用传参 | ✅ **一等公民** |
| 框架绑架程度 | 中 | 高 | 中 | 低 | **零** |
| 是否违背"从底层写起" | 部分（图运行时） | 是（全栈框架） | 是（完整 runtime） | 是（provider 抽象） | **否** |
| 适合本项目？ | 可作参考 | 不适合做内核 | 可作参考/fork 蓝本 | 可作 UI 层工具 | **内核采用** |

### 4.3 推荐方案

**内核层：纯自研 thin core + 最小依赖集**

理由：

1. **目标明确要求"从调用大语言模型提供商底层接口开始编写 agent 逻辑"** —— Mastra / OpenAI Agents SDK 这类完整框架会替你把 loop、工具、会话全做了，等于又引入一个"eve"；
2. 框架对比显示：能提供动态配置的框架（Mastra、Agno、OpenAI Agents）都是"完整 runtime"，要么 loop 固定（OpenAI Agents），要么生态绑定（Mastra）；而构建块（LangGraph）又引入图运行时心智，对"一个简单 agent loop"是过度设计；
3. **真正的复杂度不在 loop，而在 provider 差异、流式解析、schema 校验** —— 这三块都有极小的专用库可复用（见下），自己写 loop 本体只需数百行；
4. 参考价值保留：**OpenAI Agents SDK 的 loop 实现**（代码量小、MIT）可作为写 loop 的蓝本；LangGraph 的 checkpointer 思想可参考；Mastra 的 memory 设计可参考。

**内核依赖清单（全部 MIT/Apache，无框架绑架）：**

| 依赖 | 用途 | 说明 |
|---|---|---|
| `zod` 4 | 工具入参 schema 定义 + 运行时校验 | 团队已在 eve-agent 中使用；v4 内置 `z.toJSONSchema()` 转 JSON Schema 发给 LLM。备选 `@sinclair/typebox`（原生 JSON Schema 输出，更贴合 LLM API，可后续评估） |
| `eventsource-parser` 3 | OpenAI 兼容 SSE 流解析 | Vercel 维护，轻量 ~1KB，处理非规范 SSE 容错 |
| `@modelcontextprotocol/sdk` | MCP client（可选，M1 引入） | 动态接入外部工具的标准协议 |
| （无） | fetch / AbortSignal / ReadableStream | Node 24+ 原生能力，直接调 OpenAI 兼容端点与 Anthropic 原生 API |

> **关于"技术栈试用 javascript"的说明**：JS 生态的事实标准是 **TypeScript（strict）**。eve-agent 已是 TS 代码库，工具定义、配置数据模型都需要类型。因此内核用 **TypeScript 编写**（即"带类型的 JavaScript"），产物可被纯 JS 消费。若坚持纯 JS，可用 JSDoc + `checkJs` 降级，但不推荐。

**运行时：Node.js 24 LTS+**（本机 v26.0.0 满足；需要 `worker_threads` 做工具执行隔离、与 Electron 兼容、npm 生态完整）。

**仓库结构：根目录多项目文件夹（暂不引入 workspace 工具链）**——`one-agent-app/`、`one-agent-core/`、`one-agent-agent-py/` 三个平级目录 + 根级 `contracts/`，详见 [[#4.5 仓库结构选型：多项目文件夹（不引入 workspace 工具链）]]。

**应用层：Next.js 16.3 App Router + React 19 + Tailwind 4 + shadcn/ui**（沿用 eve-agent 现有 UI 栈，迁移成本低）。

**应用层是否用 Vercel AI SDK？** v0.1 **不用**。内核导出标准 SSE 流协议，应用层 Route Handler 透传、前端自研轻量流式消费（约 50 行）。保持端到端可控、依赖最小。后续如需 generative UI（模型生成 React 组件）、自动重试等能力，再引入 `ai` 7 做 UI 层增强（内核不变）。

---

### 4.4 多语言（Python）兼容

**结论：可以兼容，且应该从架构上支持"内核语言无关"。**

**工具层面的兼容方案（若未来引入 workspace 工具链）：**

| 工具 | 对 Python 的支持 | 结论 |
|---|---|---|
| pnpm workspaces | 只管理 JS 包；Python 包（`pyproject.toml`）在同一仓库共存，`pnpm-workspace.yaml` 的 glob 只包含 JS 目录 | ✅ 可行 |
| uv（Astral） | Python 侧包管理 + workspace（`uv workspaces` 管 Python 子包），2026 年 Python 包管理事实标准之一 | ✅ 与 pnpm 互补 |
| Turborepo 2 | **语言无关**的任务运行器：turbo.json 的任务就是任意 shell 命令，`uv run pytest` / `python -m pytest` 同样有缓存与依赖图 | ✅ 推荐 |
| Nx | 一等 Python 支持：`@nxlv/python` 插件（v23.x，2026-07 仍活跃），跨语言 project graph | ✅ 备选（更重，两包场景暂不需要） |

**推荐组合（工具层面）**：JS 项目各自独立安装依赖（npm/pnpm 均可，互不干扰）；Python 侧用 uv 管理；**不引入 workspace 工具链**（见 [[#4.5 仓库结构选型：多项目文件夹（不引入 workspace 工具链）]]）。pnpm 不认识 Python 依赖没关系，`one-agent-agent-py` 由 uv 管理；三个项目的并行 dev/test 用根 package.json 的 npm scripts 编排。

**真正的架构问题：内核契约必须语言无关。** monorepo 工具只是"放得下"，要让 TS 内核与 Python 内核可互换、可共存，必须把以下契约定义为**语言中立**（详见 [[#6.8 语言无关内核契约（Python 内核兼容）]]）：

1. `AgentConfig`：以 **JSON Schema** 为唯一事实源（TS 侧 zod 生成、Python 侧 pydantic 消费，双向兼容）
2. 流协议：SSE + JSON `StreamChunk`（本就语言无关）
3. 工具协议：JSON Schema 参数 + MCP（本就语言无关）
4. 存储：SQLite / Postgres（语言无关）
5. 进程边界：内核可运行在独立进程/服务（kernel-server 模式），通过 HTTP 暴露同一契约 → **Python 内核可作为 drop-in 替代**

### 4.5 仓库结构选型：多项目文件夹（不引入 workspace 工具链）

**结论：采用根目录多项目文件夹结构，不引入 pnpm workspaces / Turborepo。** "三个平级目录"的方案成立，且对这个规模更简单。

**对比：**

| 维度 | 根目录多项目文件夹（推荐） | pnpm workspaces + Turborepo |
|---|---|---|
| 结构 | `one-agent-app/`、`one-agent-core/`、`one-agent-agent-py/`、`contracts/` 平级 | `apps/*`、`packages/*` |
| 依赖管理 | 各项目独立 package.json / pyproject.toml + 各自 lockfile | 统一 lockfile、`workspace:*` 协议 |
| 跨项目引用 | app 用 `file:../one-agent-core`（或走 HTTP） | `workspace:*` 直接引用 |
| 任务编排 | 根 package.json 的 npm scripts（concurrently / `--prefix`） | turbo 缓存 + 依赖图 |
| 适用场景 | 2 个 JS 项目 + 1 个 Python 项目、契约稳定 | 3+ JS 包、共享代码多、频繁跨包重构 |
| 复杂度 | 低 | 中 |

**为什么当前可以不用 workspace 工具链：**

1. 架构上应用层只依赖 `KernelClient` 接口 + SSE/HTTP 契约（§6.8），**跨项目边界是协议而不是包引用**，不需要 workspace 的强耦合；
2. Python 内核天然是独立服务，与 JS 包管理完全无关，用文件夹隔离反而更清晰；
3. 三个项目用根 package.json 的 npm scripts 就能编排（见 §9），无需 turbo；
4. **未来需要时迁移成本低**：目录结构不变，加 `pnpm-workspace.yaml` + turbo.json 即可，代码零改动。

**app 引用 TS 内核（本地联调）**：

- `one-agent-app/package.json`：`"@one-agent/core": "file:../one-agent-core"`
- Next.js `next.config.ts`：`transpilePackages: ['@one-agent/core']`（直接消费内核 TS 源码，免构建 watch）
- 备选：`npm link`；或直接切换 `HttpKernel`（HTTP 模式，完全无本地依赖问题，也是未来 Python 内核的接入方式）

---

## 5. 总体架构

```
┌──────────────────────────────────────────────────────────────┐
│                        one-agent/                              │
│   根目录多项目文件夹（无 workspace 工具链）；contracts/ 共享契约  │
│                                                               │
│  ┌────────────────────────────┐   ┌─────────────────────────┐ │
│  │  one-agent-app (应用层)     │   │  one-agent-core (内核)  │ │
│  │  Next.js 16 + React 19     │   │  (TS, 纯逻辑, 零 UI)      │ │
│  │                            │   │                         │ │
│  │  · 对话页 /chat            │──▶│  · Provider 层           │ │
│  │  · Agent 管理页 /agents    │   │  · ToolRegistry          │ │
│  │  · 设置页（provider 管理）  │   │  · Agent / AgentLoop     │ │
│  │  · API Routes (SSE 桥)     │   │  · Session / Storage     │ │
│  │  · SQLite/Postgres 持久化  │   │  · Stream 协议           │ │
│  └────────────────────────────┘   └─────────────────────────┘ │
│            │                            │                    │
│            └────────── SSE/JSON ────────┘                    │
│        （file: 依赖 in-process；可选 HttpKernel 切换）         │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  one-agent-agent-py（未来，Python 内核，drop-in 同一契约）│ │
│  │  LLM Providers: OpenAI 兼容 + Anthropic 原生              │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**分层原则**：
- `agent-core` **零 React 依赖、零 Next.js 依赖、零框架依赖**，只依赖第 4.3 节的最小基础库；
- 应用层只通过内核公开的 `KernelClient` 接口（§6.8）与 SSE 流协议交互；
- 内核不感知 HTTP、不感知 UI、不感知数据库实现（Storage 是接口）；
- **内核契约语言中立**：`AgentConfig`（JSON Schema）、流协议（SSE）、工具（JSON Schema + MCP）均可被未来 Python 内核无缝实现（§6.8）。

---

## 6. 内核层设计（agent-core）

### 6.1 核心抽象总览

```
AgentConfig（纯 JSON 数据）
    │ 实例化 / updateConfig / run(override)
    ▼
Agent ──▶ ToolRegistry（运行时可变）
    │         ├─ Tool { name, description, inputSchema, execute }
    │         └─ McpBridge（可选）
    │
    ├─▶ Provider（LanguageProvider 接口）
    │         ├─ OpenAICompatibleProvider
    │         └─ AnthropicProvider
    │
    ├─▶ SessionStore（接口：内存/文件/SQLite/Postgres）
    │
    └─▶ run(messages, options) → AsyncIterable<StreamChunk>
```

### 6.2 Provider 层（对接 LLM 底层接口）

**设计目标：一个统一接口，屏蔽 OpenAI 与 Anthropic 的协议差异；provider 配置全部来自运行时数据。**

```ts
// 统一内部消息格式（内核自己的消息模型，屏蔽 provider 差异）
interface LLMMessage {
  role: "system" | "user" | "assistant" | "tool";
  content: string | ContentPart[];
  toolCalls?: ToolCall[];   // assistant 发起
  toolCallId?: string;      // tool 回执
}

// 统一流式 chunk（内核 → 上层）
type StreamChunk =
  | { type: "text"; delta: string }
  | { type: "tool_call"; id: string; name: string; input: unknown }
  | { type: "tool_result"; id: string; ok: boolean; output: unknown }
  | { type: "usage"; inputTokens: number; outputTokens: number }
  | { type: "error"; message: string }
  | { type: "done" };

interface ProviderConfig {
  provider: "openai-compatible" | "anthropic";
  baseUrl: string;
  apiKey?: string;
  modelId: string;
  temperature?: number;
  maxTokens?: number;
  // 其他 provider 特定参数...
}

interface LanguageProvider {
  chat(opts: {
    messages: LLMMessage[];
    tools: ToolSpec[];          // 每次调用传入 → 天然支持动态工具
    config: ProviderConfig;     // 每次调用传入 → 天然支持动态模型
    signal?: AbortSignal;
  }): AsyncIterable<StreamChunk>;
}
```

**实现要点**：

- `OpenAICompatibleProvider`：`POST {baseUrl}/chat/completions`，`stream: true`，`stream_options: { include_usage: true }`；工具走 **function calling** 格式；用 `eventsource-parser` 解析 `data:` 行；兼容 DeepSeek / Ollama / LM Studio / vLLM / OpenAI 等一切 OpenAI 兼容端点；
- `AnthropicProvider`：`POST https://api.anthropic.com/v1/messages`，工具走 **tool_use** 格式（`input_schema`），解析 `content_block_delta` / `content_block_stop` 流事件；
- **两种 provider 输出统一映射为 `StreamChunk`**，上层完全无感；
- 非流式调用：内部聚合流为完整消息即可，接口不变；
- 请求日志：复用 eve-agent 的 `loggingFetch` 思路（可选中间件），便于调试；
- 断线/错误：统一映射为 `{ type: "error" }` chunk + 可重试标记。

### 6.3 工具系统（ToolRegistry）

**设计目标：工具的增删改是运行时操作，每个 Agent 实例拥有独立 registry。**

```ts
interface ToolSpec {
  name: string;
  description: string;
  inputSchema: JSONSchema;          // zod 定义后 z.toJSONSchema() 生成
  execute(ctx: ToolContext, input: unknown): Promise<ToolResult>;
  meta?: {
    dangerous?: boolean;            // 危险操作标记（后续接审批）
    timeoutMs?: number;
    sandbox?: boolean;              // 是否在 worker 中执行
  };
}

class ToolRegistry {
  list(): ToolSpec[];
  get(name: string): ToolSpec | undefined;
  add(spec: ToolSpec): void;            // 运行时动态注册
  remove(name: string): void;           // 运行时动态注销
  update(name: string, patch: Partial<ToolSpec>): void;
  async execute(ctx: ToolContext, call: ToolCall): Promise<ToolResult>;
}
```

**内置工具（v0.1）**：`web_search`、`read_local_file`、`write_local_file`、`run_local_command`、`rag_search`（迁移自 eve-agent 的 `agent/tools/`，改为纯数据注册）。

**MCP 桥接（M1 引入）**：

```ts
class McpBridge {
  async connect(serverConfig): Promise<void>;  // @modelcontextprotocol/sdk Client
  // 将 MCP server 的 tools 动态注册进 ToolRegistry
  // server 断开时自动移除
}
```

### 6.4 Agent 实例与动态配置

**设计目标：AgentConfig 是一等数据（JSON），不是代码。这是与 eve 最根本的区别。**

```ts
// AgentConfig —— 可序列化、可存 DB、可被 UI 编辑
interface AgentConfig {
  id: string;
  name: string;
  instructions: string;              // system prompt
  model: ProviderConfig;             // 动态：可随时改
  tools: Array<{ name: string; enabled: boolean }>;  // 动态：可增删
  memory?: {
    strategy: "none" | "window" | "compaction";
    maxMessages?: number;
    thresholdPercent?: number;       // compaction 触发阈值
  };
  maxIterations?: number;            // 默认 10
  temperature?: number;
}

class Agent {
  constructor(config: AgentConfig, deps: { providerFactory, toolRegistry, sessionStore });
  getConfig(): AgentConfig;
  updateConfig(patch: Partial<AgentConfig>): void;   // ★ 运行时热更新
  run(input: string | Message[], opts?: {
    modelOverride?: Partial<ProviderConfig>;  // ★ 每会话/每请求覆盖
    toolOverrides?: { name: string; enabled: boolean }[];
    sessionId?: string;
    signal?: AbortSignal;
  }): AsyncIterable<StreamChunk>;
}
```

**动态配置的使用场景（对应 eve 做不到的事）**：

| 场景 | eve（静态） | one-agent（动态） |
|---|---|---|
| 用户 A 用 DeepSeek，用户 B 用 Claude | ❌ 需 hack | ✅ 每会话 `modelOverride` |
| 对话中切换模型 | ❌ 重启/改代码 | ✅ `agent.updateConfig()` |
| 给 Agent 动态加一个工具 | ❌ 写文件重建 | ✅ `registry.add()` |
| UI 上创建/编辑 Agent | ❌ 文件系统 | ✅ 表单 → JSON → `new Agent()` |
| 多个 Agent 实例 | ❌ 目录复制 | ✅ 任意多实例，共享 provider |

### 6.5 Agent Loop（ReAct 风格，自研）

```ts
async function* run(agent: Agent, messages: LLMMessage[], opts): AsyncIterable<StreamChunk> {
  const maxIter = agent.getConfig().maxIterations ?? 10;
  for (let i = 0; i < maxIter; i++) {
    // 1) 组装：instructions + 历史消息 + 当前工具列表
    const stream = agent.provider.chat({
      messages: buildPrompt(agent, messages),
      tools: agent.registry.list().filter(t => enabled),
      config: { ...agent.getConfig().model, ...opts.modelOverride },
      signal: opts.signal,
    });
    // 2) 流式转发 + 聚合
    let text = "";
    const toolCalls: ToolCall[] = [];
    for await (const chunk of stream) {
      yield chunk;                       // 实时转发给上层/前端
      if (chunk.type === "text") text += chunk.delta;
      if (chunk.type === "tool_call") toolCalls.push(...);
    }
    // 3) 无工具调用 → 回复完成
    if (toolCalls.length === 0) { yield { type: "done" }; return; }
    // 4) 执行工具（可并行），结果回填消息
    for (const tc of toolCalls) {
      const result = await agent.registry.execute(ctx, tc);
      yield { type: "tool_result", id: tc.id, ok: result.ok, output: result.output };
      messages.push(toolResultMessage(tc, result));
    }
    // 5) 记忆管理：窗口裁剪 / compaction（超过阈值时摘要旧消息）
    messages = maybeCompact(agent, messages);
  }
  yield { type: "error", message: "max iterations reached" };
}
```

要点：
- 支持 `AbortSignal` 取消（前端"停止生成"按钮）；
- 工具执行可配置并行/串行；
- 预留 hook 点：`onTurnStart` / `onToolCall` / `onToolResult` / `onError`（可观测性、审计）。

### 6.6 会话与存储

```ts
interface Message { id, role, content, toolCalls?, toolResults?, createdAt, usage? }
interface Session { id, agentId, messages: Message[], createdAt, updatedAt, meta? }

// 内核只定义接口，应用层提供实现（SQLite/Postgres/内存）
interface SessionStore {
  createSession(s: Session): Promise<void>;
  getSession(id: string): Promise<Session | null>;
  appendMessage(sessionId: string, m: Message): Promise<void>;
  listSessions(agentId?: string): Promise<Session[]>;
  deleteSession(id: string): Promise<void>;
}
```

### 6.7 内核对外 API（index.ts 导出面）

```ts
export { Agent } from "./agent/Agent";
export type { AgentConfig } from "./agent/types";
export { ToolRegistry, type ToolSpec, type ToolContext } from "./tools/ToolRegistry";
export { OpenAICompatibleProvider, AnthropicProvider } from "./providers";
export type { LanguageProvider, ProviderConfig, StreamChunk, LLMMessage } from "./types";
export { InMemorySessionStore, type SessionStore, type Session, type Message } from "./session";
export { McpBridge } from "./mcp/McpBridge";   // M1
```

### 6.8 语言无关内核契约（Python 内核兼容）

**目标**：未来用 Python 重写/并行实现内核时，应用层无感知。JS 内核与 Python 内核是**同一契约的两个实现**。

**契约清单（全部语言中立）：**

| 契约 | 载体 | JS 实现 | Python 实现 |
|---|---|---|---|
| AgentConfig / 配置 | **JSON Schema（唯一事实源）** | zod v4 → `z.toJSONSchema()` | pydantic v2（从 JSON Schema 生成模型） |
| 流式输出 | SSE + JSON `StreamChunk` | 自研 / eventsource-parser | httpx-sse / sse-starlette |
| 工具参数 | JSON Schema（发给 LLM 的格式） | zod / typebox | pydantic / jsonschema |
| 工具互操作 | MCP（协议级标准） | @modelcontextprotocol/sdk | mcp（官方 Python SDK） |
| 会话存储 | SQLite / Postgres | better-sqlite3 / pg | sqlite3 / psycopg |

**进程边界设计（关键）**：

```
应用层（Next.js）──▶ KernelClient 接口（语言无关）
                      ├─ InProcessKernel ──▶ @one-agent/core（TS 内核，v0.1 默认）
                      └─ HttpKernel ───────▶ agent-core-server（HTTP 服务）
                                               ├─ TS 内核实现（Node）
                                               └─ Python 内核实现（未来，drop-in）
```

```ts
// KernelClient —— 应用层唯一依赖的内核面（v0.1 放 @one-agent/core 导出）
interface KernelClient {
  runChat(req: {
    agentConfig: AgentConfig;        // JSON 数据，语言无关
    message: string | Message[];
    sessionId?: string;
    signal?: AbortSignal;
  }): AsyncIterable<StreamChunk>;    // 统一流事件，语言无关
  // 配置 CRUD（可选，v0.1 由应用层直接管理 AgentConfig）
}
```

- v0.1 用 `InProcessKernel`（零额外部署，进程内直调）；
- `HttpKernel` 为可选部署模式（`one-agent-core/server/`，把 §8.2 的 SSE 协议原样暴露为 HTTP 服务）——此时应用层与内核解耦成独立服务，**Python 内核只需实现同一 HTTP 契约即可无缝替换**；
- **契约测试**：`contracts/` 存放 AgentConfig 的 JSON Schema + 流协议样例（同一输入 → 同一事件序列），做跨语言一致性测试，防止 JS/Python 两种实现漂移。

---

## 7. 应用层设计（one-agent-app）

### 7.1 技术栈

- Next.js 16.3（App Router）+ React 19 + TypeScript
- Tailwind CSS 4 + shadcn/ui（沿用 eve-agent 的 UI 栈）
- 持久化：SQLite（本地，`better-sqlite3`）起步，预留 Postgres（部署）
- 状态管理：React Query（服务端状态）+ 轻量本地状态

### 7.2 页面与路由

| 路由 | 功能 |
|---|---|
| `/` | 重定向到最近会话或 Agent 列表 |
| `/chat/[sessionId]` | 对话页：消息流式渲染、工具调用状态卡片、停止/重试、切换模型 |
| `/agents` | Agent 列表 |
| `/agents/new` | 创建 Agent：表单动态配置 model（provider/baseUrl/modelId/温度）+ tools 勾选 + instructions |
| `/agents/[id]` | 编辑 Agent（热更新生效）/ 查看会话历史 |
| `/settings/providers` | provider 凭据管理（apiKey 加密存储，模型配置可复用） |

### 7.3 API 设计

```
POST /api/chat                      # SSE 流式对话
  body: { sessionId?, agentId, message, modelOverride?, toolOverrides? }
  resp: text/event-stream（内核 StreamChunk → SSE，见 §8）

GET/POST /api/agents                # 列表 / 创建
GET/PATCH/DELETE /api/agents/[id]   # 详情 / 热更新 / 删除
GET/POST /api/sessions              # 会话列表 / 新建
GET /api/sessions/[id]              # 会话消息（含工具调用记录）
GET/POST/PATCH /api/providers       # provider 凭据管理
```

**对话流（核心链路）**：

```
浏览器 ──POST /api/chat──▶ Route Handler
                              │ 读取 agentId → 从缓存获取 Agent 实例
                              │ （未命中则 new Agent(config) 并缓存）
                              │ run({ message, modelOverride, sessionId })
                              ▼
                        AsyncIterable<StreamChunk>
                              │ 编码为 SSE data: 行
                              ▼
浏览器 fetch + ReadableStream 解析 → 流式渲染
```

- Agent 实例按 `agentId` 缓存于进程内；`PATCH /api/agents/[id]` 后失效重建 → **UI 改配置即时生效**；
- 前端流式消费：自研 ~50 行解析器（`fetch` + `ReadableStream` + `TextDecoder` 按 `\n\n` 切 `data:` 行），不引入 AI SDK；
- 对话页展示工具调用过程（`tool_call` / `tool_result` chunk → 可折叠卡片）。

---

## 8. 数据模型与流协议

### 8.1 AgentConfig 示例（存 DB 的 JSON）

```json
{
  "id": "agent-demo",
  "name": "演示助手",
  "instructions": "你是一个乐于助人的中文助手。使用工具时先说明你的计划。",
  "model": {
    "provider": "openai-compatible",
    "baseUrl": "https://api.deepseek.com/v1",
    "modelId": "deepseek-chat",
    "apiKeyRef": "provider:deepseek:apiKey",
    "temperature": 0.7,
    "maxTokens": 4096
  },
  "tools": [
    { "name": "web_search", "enabled": true },
    { "name": "read_local_file", "enabled": true },
    { "name": "run_local_command", "enabled": false }
  ],
  "memory": { "strategy": "compaction", "thresholdPercent": 0.75 },
  "maxIterations": 10
}
```

> 说明：`apiKey` 不明文存 AgentConfig，用 `apiKeyRef` 引用（环境变量或 `/settings/providers` 里加密存储的凭据）。

### 8.2 流协议（内核 ↔ 应用层 ↔ 前端）

格式：**HTTP POST + SSE**（`text/event-stream`），每行 `data: <json>`，与 OpenAI 流式风格一致，便于任何客户端复用：

```text
data: {"type":"text","delta":"你好"}

data: {"type":"tool_call","id":"call_1","name":"web_search","input":{"q":"2026 诺贝尔奖"}}

data: {"type":"tool_result","id":"call_1","ok":true,"output":"..."}

data: {"type":"text","delta":"根据搜索结果..."}

data: {"type":"usage","inputTokens":1234,"outputTokens":567}

data: {"type":"done","sessionId":"s_1","messageId":"m_9"}
```

终止：`{"type":"done"}`；错误：`{"type":"error","message":"..."}`；客户端可 `AbortController` 中断（服务端 `AbortSignal` 传导）。

---

## 9. 项目目录结构（根目录多项目文件夹）

```
one-agent/
├── package.json                  # 根级编排脚本（并行 dev/test 三个项目，见下）
├── README.md
├── .gitignore
├── contracts/                    # ★ 语言无关契约（唯一事实源）
│   ├── agent-config.schema.json  # AgentConfig 的 JSON Schema（zod 生成，pydantic 消费）
│   └── stream-protocol.md        # 流协议文档 + 事件样例（跨语言契约测试用）
├── one-agent-app/                # 应用层（Next.js 16，独立 package.json + lockfile）
│   ├── package.json              # name: one-agent-app；deps 含 "@one-agent/core": "file:../one-agent-core"
│   ├── next.config.ts            # transpilePackages: ['@one-agent/core']
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── chat/[sessionId]/page.tsx
│   │   ├── agents/page.tsx
│   │   ├── agents/new/page.tsx
│   │   ├── agents/[id]/page.tsx
│   │   ├── settings/providers/page.tsx
│   │   └── api/
│   │       ├── chat/route.ts           # SSE
│   │       ├── agents/route.ts
│   │       ├── agents/[id]/route.ts
│   │       ├── sessions/route.ts
│   │       ├── sessions/[id]/route.ts
│   │       └── providers/route.ts
│   ├── components/              # 对话流、工具卡片、Agent 表单
│   └── lib/                     # db（SQLite）、agent-cache、sse、kernel-client
├── one-agent-core/              # 内核层（TS，@one-agent/core，独立 package.json + lockfile）
│   ├── package.json
│   ├── tsconfig.json
│   ├── src/
│   │   ├── index.ts             # 对外 API 面（含 KernelClient）
│   │   ├── types.ts             # LLMMessage / StreamChunk / ProviderConfig
│   │   ├── providers/
│   │   │   ├── types.ts
│   │   │   ├── openai-compatible.ts
│   │   │   └── anthropic.ts
│   │   ├── tools/
│   │   │   ├── ToolRegistry.ts
│   │   │   ├── schemas.ts       # zod schemas（输出 JSON Schema 到 contracts/）
│   │   │   └── builtin/         # web_search / read_local_file / ...
│   │   ├── agent/
│   │   │   ├── Agent.ts
│   │   │   ├── AgentLoop.ts
│   │   │   └── config.ts        # AgentConfig 校验（zod）
│   │   ├── session/
│   │   │   ├── types.ts
│   │   │   ├── InMemoryStore.ts
│   │   │   └── interfaces.ts
│   │   ├── memory/              # 窗口裁剪 / compaction
│   │   └── mcp/                 # McpBridge（M1）
│   ├── server/                  # agent-core-server（可选 HTTP 模式，§6.8）
│   │   └── index.ts             # 暴露 §8.2 SSE 协议
│   └── tests/                   # vitest
└── one-agent-agent-py/           # Python 内核（未来，drop-in 实现同一契约）
    ├── pyproject.toml           # uv 管理
    └── src/one_agent_core/      # providers / tools / agent / session / server
```

**根级编排脚本（package.json 示例）**：

```json
{
  "name": "one-agent",
  "private": true,
  "scripts": {
    "dev": "concurrently -n app,kernel -c blue,green \"npm --prefix one-agent-app run dev\" \"npm --prefix one-agent-core run dev\"",
    "dev:app": "npm --prefix one-agent-app run dev",
    "dev:kernel": "npm --prefix one-agent-core run dev",
    "test": "npm --prefix one-agent-core run test && npm --prefix one-agent-app run test",
    "test:py": "cd one-agent-agent-py && uv run pytest"
  },
  "devDependencies": { "concurrently": "^9" }
}
```

---

## 10. 里程碑规划

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **M0 脚手架** | 项目骨架（根目录三项目 + `contracts/`）、`one-agent-core` 内核骨架、Provider 层 | `node` 脚本能流式调用 DeepSeek/Ollama 任一兼容端点 + Anthropic，输出 text/usage chunk |
| **M1 工具与 loop** | ToolRegistry、Agent 类、AgentLoop、内置工具 | CLI 对话：多轮工具调用（web_search → 回答）流式跑通 |
| **M2 会话与记忆** | Session/Message、SessionStore（内存+SQLite）、窗口裁剪/compaction | 长对话不爆上下文，重启后能恢复会话 |
| **M3 应用层 v1** | Next.js 对话页 + SSE 桥 + Agent 管理页（动态配置 UI） | 浏览器里：创建 Agent → 配置模型/工具 → 对话流式渲染 |
| **M4 动态配置强化** | per-session override、Agent 热更新、多用户模型隔离 | 同一服务不同会话用不同模型/工具，无需重启 |
| **M5 增强** | MCP 桥接、工具沙箱（worker_threads）、可观测性（请求日志/OTel）、API key 加密存储 | 外部 MCP server 工具动态接入；危险工具审批 hook |
| **M6（可选）** | 冻结契约 v1 → Python 内核（`one-agent-agent-py`）实现同一契约 | 契约测试通过：同输入 → 与 TS 内核一致的事件序列；应用层可切换 `HttpKernel` 指向 Python 服务 |

> 建议：M0–M2 全部在 `agent-core` 内用 vitest + CLI 脚本验证（内核不依赖 UI），M3 再动 Next.js。

---

## 11. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 从零写 provider 层，两家 API 细节差异多（消息格式、工具格式、流事件） | 开发量 | 统一内部消息模型消化差异；先做 OpenAI 兼容（覆盖 DeepSeek/Ollama/OpenAI），Anthropic 其次；参考 OpenAI Agents SDK 的 provider 层实现 |
| SSE 解析边界（多字节、分包、异常流） | 流式不稳 | 用 `eventsource-parser` + 自封装 `TextDecoder({stream:true})`，写单测覆盖边界 |
| compaction 摘要质量影响长对话效果 | 体验 | 策略可插拔（window/compaction/none），后续可换摘要模型或向量记忆 |
| 动态配置带来安全隐患（用户可配任意 baseUrl/模型/工具） | 安全 | v0.1 单用户；apiKey 引用式存储不落明文；危险工具 `meta.dangerous` 标记 + 审批 hook 预留 |
| 多 Agent 实例内存占用 / 缓存失效 | 性能 | Agent 实例按需创建 + LRU 缓存；配置变更版本号失效 |
| 内核多语言（JS + Python）导致两种实现漂移 | 一致性 | `contracts/` 作为唯一事实源 + 跨语言契约测试（同一输入 → 同一事件序列）；启动 Python 内核前先冻结契约 v1 |
| app 与内核本地联调（`file:` 依赖 + transpilePackages）偶有缓存/路径问题 | 开发体验 | 保留 `KernelClient` 接口；问题频繁时切换 `HttpKernel`（HTTP 模式无本地依赖问题），也是 Python 内核的接入方式 |
| 重写范围过大，偏离"从底层写起"目标 | 进度 | 严格按里程碑：M0–M2 只做内核，禁止提前引入框架依赖 |

---

## 附：参考材料

- eve 框架文档：`~/codes/eve-agent/node_modules/eve/docs/`（agent-config.md / concepts / tools）
- eve-agent 现有 hack 代码：`~/codes/eve-agent/lib/model-config.ts`、`lib/model-context.ts`、`agent/agent.ts`
- MCP 规范：https://modelcontextprotocol.io
- Vercel AI SDK：https://ai-sdk.dev（仅作 UI 层增强备选）
- 本机环境：Node v26.0.0 / npm 11.12.1 / Next 16.3.0 可用

---

*下一步：评审本方案 → 确认技术选型 → 初始化 monorepo（M0）。*
